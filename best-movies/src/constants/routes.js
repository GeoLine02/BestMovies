const home = "/";
const signIn = "/signIn";
const SignUp = "/signUp";
const favorites = "/favorites";
const movies = "/movies";

const routes = {
  home,
  signIn,
  SignUp,
  favorites,
  movies,
};

export default routes;
